# C4 — An instrument

> **TODO (James, before the Monday 12:00 cutoff):** this file is mine to write —
> 150–300 words, in my own words. The two prompts below are the standing ones.
> The notes under each are just the raw material from this week's session, not
> the reflection. Replace them.

## What was the breakthrough that moved the work forward?

<!--
  Raw material from this week, to draw on — not the answer:

  - The front-loaded question batch. I answered ~12 questions in one message
    (instrument concept, physics of the beam, timbre direction, scope) before a
    line of code existed. Compare with how earlier weeks trickled decisions out.
  - The pendulum. I described the beam's motion physically — ropes, a pivot,
    circular arc, gravity, release height — rather than describing an effect I
    wanted. That description was directly implementable.
  - Karplus-Strong vs. additive-inharmonic. I asked for K-S; the agent pushed
    back that K-S is a plucked-string algorithm producing harmonic partials,
    and that a bell's character is its *inharmonicity*. I took the correction.
    Worth deciding whether that counts as the breakthrough or as a correction I
    made room for.
-->

_(my answer here)_

## What did this work change about who I want to be as a software developer?

<!--
  Raw material:

  - Two pure modules (`src/bell.ts`, `src/pendulum.ts`) with no DOM and no
    AudioContext, so the spec tests could assert contracts — "a bigger bell is
    lower", "one release, one strike". 15 of those passed before any page
    existed.
  - The limit of that: no test can tell me whether it sounds like a bell, or
    whether a stranger finds music in it. I still had to open it and listen.
    What does that split mean for how I want to work?
-->

_(my answer here)_
